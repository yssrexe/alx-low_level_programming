#include <stdio.h>
/**
 * main - name of program
 * @argc: argument count
 * @argv: argument victor
 * Return: return 0 
 */

int main(int argc, char **argv)
{
        printf("%s\n", argv[0]);
        (void)argc;
        return(0);
}
