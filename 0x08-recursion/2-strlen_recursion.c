#include "main.h"
/**
 * _print_rev_recursion - prints a string in reverse
 * @s: string
 * Return: nothing
 */

int _strlen_recursion(char *s)
{
    int i = 0;

    while (s[i])
    {
        i++;
    }
    return(i);
}
