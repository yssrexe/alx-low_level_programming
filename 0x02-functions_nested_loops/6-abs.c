/**
 * _abs - Calculate the absolute value of a given number
 * @n: number to be calculated
 * Return: a number
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
